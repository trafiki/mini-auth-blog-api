{
  "name": "mini-blog-auth-app",
  "version": "1.0.0",
  "description": "A mini blog app to demonstrate auth with social login",
  "main": "app.js",
  "scripts": {
    "build": "babel server.js --out-dir build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run build && node ./build/app.js",
    "start:dev": "nodemon --exec babel-node ./backend/app",
    "migrate": "babel-node ./backend/database/migrations/createTables"
  },
  "author": "Akerele Babatunde",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^4.0.1",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "jsonwebtoken": "^8.5.1",
    "pg": "^8.0.3"
  }
}
